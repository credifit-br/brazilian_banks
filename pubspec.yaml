name: brazilian_banks
description: Get a list of brazilian bank codes/names, and account/digit validation (Retorna a lista de bancos brasileiros, com seus nomes e códigos, e faz validação de dígito de contas)
version: 0.5.3
homepage: https://github.com/credifit-br/brazilian_banks

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.3"

dependencies:
  http: ">=0.13.1 <1.0.0"
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^1.0.1
  flutter_test:
    sdk: flutter

flutter:
